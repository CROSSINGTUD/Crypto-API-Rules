SPEC java.security.spec.EncodedKeySpec

OBJECTS 	
	byte[] encodedKey;

EVENTS
	con: EncodedKeySpec(encodedKey);
	
ORDER
 	con

REQUIRES
	preparedKeyMaterial[encodedKey];
 	
ENSURES
	speccedPrivKeySpec[this];
	speccedPubKeySpec[this];
	